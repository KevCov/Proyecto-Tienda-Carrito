import { NameSearchPipePipe } from './name-search-pipe.pipe';

describe('NameSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NameSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
